# Student UI kit (port 3002)

Three stacked cards per the brief's State C (Issued):
1. **DID card** (gradient hero — only gradient in the system)
2. **Encrypted credential** (Fetch + decrypt, collapsible JSON)
3. **Access grants** (form + active list with countdown)

Accent: emerald (`--student`).
