# University UI kit (port 3003)

DID-gated transcript portal. CGPA hero chip, clean table of courses. Every render is gated by `hasAccess(student, platform)` on-chain.

Accent: violet (`--university`).
